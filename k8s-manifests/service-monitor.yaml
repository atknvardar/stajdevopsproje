apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: weather-app-monitor
  namespace: weather-app
  labels:
    app: weather-app
spec:
  selector:
    matchLabels:
      app: weather-app
  endpoints:
  - port: 8080-tcp
    path: /metrics
    interval: 30s
    scheme: http